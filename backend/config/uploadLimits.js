const KB = 1024;
const MB = 1024 * 1024;

module.exports = {
  MAX_FILE_BYTES: 100 * KB,
  MAX_FILES_PER_REPO: 50,
  MAX_REPO_BYTES: 1 * MB,
  MAX_REPOS_PER_USER: 10,
  MAX_USER_BYTES: 5 * MB,
  MAX_PATH_LENGTH: 200,
  MAX_NAME_LENGTH: 100,
  MAX_DESCRIPTION_LENGTH: 500,
  GLOBAL_DB_LIMIT_BYTES: 400 * MB,
};
